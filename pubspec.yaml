name: permission_config
description: A Flutter CLI plugin that auto-adds Android/iOS permissions and generates permission handler logic.
version: 0.0.11

environment:
  sdk: ">=2.17.0 <4.0.0"
  flutter: ">=1.20.0"

flutter:
  plugin:
    implements: permission_config
    platforms:
      android:
        default_package: permission_config
      ios:
        default_package: permission_config

dependencies:
  path: ^1.9.0
  xml: ^6.5.0
  logger: ^2.0.2

dev_dependencies:
  test: ^1.21.0
  mockito: ^5.4.0
  build_runner: ^2.4.6

executables:
  permission_config: permission_config

repository: https://github.com/jassimpv/permissions_config
issue_tracker: https://github.com/jassimpv/permissions_config/issues
