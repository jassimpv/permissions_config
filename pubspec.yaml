name: permissions_config
version: 0.0.1
description: CLI tool to add platform permissions like camera/mic/storage.
environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  yaml: ^3.1.1
  xml: ^6.3.0
  path: ^1.9.1
  logger: ^2.6.0

dev_dependencies:
  flutter_lints: ^3.0.1

executables:
  permissions_config: permissions_config
