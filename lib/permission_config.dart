/// A Dart library for managing runtime permission configurations
/// for both Android and iOS platforms using the `permission_handler` package.
///
/// Exposes utilities to easily request permissions with rationale
/// and handle permanently denied cases.
library permission_config;

/// Exports the core permission handler utility.
export 'src/handler.dart';
